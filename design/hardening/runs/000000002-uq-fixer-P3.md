# 000000002 · uq-fixer · P3 (design/invert safety gate — LIM-013)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →`
block + its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P3 fix (commit a26378971e885c3696b3cabd3dc797a03d2232ff)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (re-scan) → P1 → P2 → … until HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated.
- **Target:** **P3** — `nudge.design.invert.design` safety gate (`NUDGE-METHOD-007`,
  `NUDGE-LIM-013`). Red-team HOLE 3 (`design/FAILSAFE_REDTEAM_3.md`).

## What it did (verbatim fix claim, condensed)

- **Independently reproduced** the deterministic confident-wrong via
  `scripts/redteam/design_safety_gate_absolute_proximity.py`: the pre-fix gate cleared
  an intervened circuit at proximity **0.5895** (`delta 0.0893 < margin 0.15`) as
  "safety: OK, stays away from the fold" while `classify_robustness` on the identical
  circuit returns `near-fold` — a confident-wrong SAFETY label on a proposal.
- **Measured root cause:** `_safety_report` set `high_risk_of_instability =
  bool(delta > margin)` — purely *relative*; the absolute `proximity_after` was never
  compared against the shipped `bifurcation.NEAR_FOLD = 0.55` (the cut
  `classify_robustness` uses). Aggravating factor: the "safe" reason branch omitted the
  one-sided-lower-bound caveat (`NUDGE-LIM-012`).
- **Fix (additive; `src/nudge/design/invert.py` ONLY — frozen core untouched):** import
  the existing `NEAR_FOLD` constant; add `SafetyReport.near_fold`; fire
  `high_risk_of_instability = (delta > margin) OR (proximity_after >= NEAR_FOLD)` in every
  branch where `proximity_after` is defined (incl. base-not-bistable → an intervention
  that *creates* a near-fold switch). `_circuit_reason` routes the near-fold case through
  wording that AGREES with `classify_robustness`, and carries the one-sided caveat on the
  SAFE branch when `proximity_after` is one-sided. No arbitrary new threshold — reuses the
  SAME constant, so the safety gate and `classify_robustness` can never disagree.
- **Status claimed:** **CLOSED** (a logic gap, genuinely closable) — a *passing* decoy,
  not a strict-xfail.
- **Honesty record:** `NUDGE-LIM-013` sharpened (two-alarm rule + safe-branch caveat,
  schema-valid); `scripts/vv/FINDINGS.md` "P3" entry (measured before/after); living docs
  (STATE, CHANGELOG, README, `docs/mechanism_cards/inverse_design.md`, module docstring).
- **Regression lock (`tests/design/test_invert.py`, passing, not slow):**
  `test_safety_gate_flags_sub_margin_push_across_near_fold`,
  `test_positive_control_robust_intervention_below_near_fold_stays_ok`,
  `test_safe_branch_carries_one_sided_lower_bound_caveat`.

## Branch + commit (NOT merged by the fixer)

- Branch: `worktree-agent-a581da60a3c70ac77`
- Commit: `a26378971e885c3696b3cabd3dc797a03d2232ff`

## Gate (as reported by the fixer — independently re-checked by audit, run 000000003)

ruff clean · pyright 0 · 4 doc checkers OK · `pytest -q` 254 passed/5 skipped/1 xfailed ·
slow+decoy (design+battery) 6 passed · repro exits "no hole".

*Environment note the fixer flagged: the fresh worktree venv ships EMPTY — `uv sync
--extra dev --extra ci` is required before the suite will run.*
