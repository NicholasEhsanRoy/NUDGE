# 000000006 · uq-fixer · P1 (differential additive-perturbed-offset confound — LIM-016)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P1 fix (commit b562da9d3a0a98ab7f39b0624f56188e9a643b8a)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (re-scan) → P2 → … until HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated (based off session-start `3fafbda`).
- **Target:** **P1** — `nudge.inference.differential.attribute_differential`
  (`NUDGE-METHOD-010`, `NUDGE-LIM-016`). Red-team HOLE 1 (`design/FAILSAFE_REDTEAM_3.md`).

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped path, `ras_switch_1node`, N=3000, 2 seeds): an
  additive offset on context B's PERTURBED cells only (control clean), `mechanism="none"`
  (truth = no-difference) → **3 confident-wrong `gain-diff` across 2 seeds** (seed0/off3
  dBIC-vs-shared 21.2; seed0/off5 201.4; seed1/off5 231.0). `depth_ratio ≈ 1.01`, so the
  control-keyed depth guard (gate 2) never engaged.
- **Measured root cause + the separation measurement (the crux):** an additive offset
  TRANSLATES the perturbed OFF baseline up vs that context's own control; a genuine
  `K`/`n`/`v_max` difference leaves the OFF mode anchored near basal. A corruption-onset
  sweep (`off_shift` = perturbed-vs-control low-quantile ratio) measured a **one-sided**
  separation: every confident-wrong offset had `off_shift ≥ 2.99`; the strongest genuine
  gain/ceiling/threshold difference (×3–4) only reached `≤ 1.96`. A *symmetric* cut would
  over-abstain (genuine reductions DEFLATE off_shift to 0.15–0.48) — the **inflation
  direction alone** is the clean separator.
- **Fix (additive; `src/nudge/inference/differential.py` ONLY — frozen core untouched):**
  new classifier **gate 4b** in `classify_differential` — before any positive `*-diff`,
  abstain (`unresolved`) when `max(off_shift_a, off_shift_b) > off_shift_max`
  (`_OFF_SHIFT_INFLATION_MAX = 2.5`, the MIDPOINT of the measured 1.96↔2.99 gap — not
  arbitrary). One-sided by construction (deflation never trips it). Threaded through
  `attribute_differential`.
- **Status:** **CLOSED for the demonstrated (inflating) offset; BOUNDED in general
  (honest).** Residual: a *deflating* perturbed-only offset (dropout-like, `off_shift < 1`)
  aliases with a genuine knob reduction and is NOT separable by this diagnostic — unguarded,
  documented loudly in `NUDGE-LIM-016`.
- **Honesty record:** `NUDGE-LIM-016` sharpened (schema-valid) with the P1 channel + the
  deflating residual bound; `scripts/vv/FINDINGS.md` §P1 (measured before/after table);
  module + `_off_baseline_shift` docstrings, Mechanism Card, STATE, CHANGELOG, README all
  corrected — `off_shift` is no longer called "non-load-bearing" (now "one-sided
  load-bearing").
- **Regression lock (`tests/inference/test_differential.py`):** the 3 confident-wrong
  configs abstain; offset-0 → `no-difference`; every positive control still resolves.

## Branch + commit (NOT merged by the fixer)

- Branch: `worktree-agent-a71d0154509451556` · Commit:
  `b562da9d3a0a98ab7f39b0624f56188e9a643b8a` (8 files, +318/−31).

## Gate (fixer-reported; independently re-checked by audit, run 000000007)

red-team script HOLES: 0 (2 seeds) · differential slow suite 11 passed (incl. genuine
ceiling/gain recovery + depth-confound abstain) · ruff · pyright 0 · 5 doc checkers ·
`pytest -q` 254 passed/5 skipped/1 xfailed.

## RESOURCE NOTE flagged by the fixer (relayed to user)

Running two JAX fit-jobs concurrently OOM-killed them on this ~16 GB / 4-core box (silent
SIGKILL, no traceback); leftover background jobs starved a repro (7× slowdown). **Run heavy
fits STRICTLY SEQUENTIALLY.** The P1 repro is ~10–13 min; the differential slow suite ~8 min.
