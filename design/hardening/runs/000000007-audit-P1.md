# 000000007 · audit · P1 (differential additive-perturbed-offset confound — LIM-016) → PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + MERGE the P1 fix (audit PASSED)
then  →  nudge-red-team (re-scan for P1-fix regressions + new holes)
then  →  nudge-uq-fixer on P2 (multi_reporter batch confound, LIM-014)
```

## Role / target

- **Role:** `nudge-audit` (role 4), worktree-isolated, independent of the fixer.
- **Target:** the P1 fix at commit `b562da9d3a0a98ab7f39b0624f56188e9a643b8a`.

## Verdict: **AUDIT: PASS**

Re-run from scratch (merged the fix commit as a clean fast-forward into a 3fafbda-based
worktree; `uv sync`). All heavy jobs run **sequentially** (OOM-aware).

- **Hole now abstains (yes).** `differential_additive_confound.py 3` → **HOLES: 0** across
  seeds **0, 1, 2** (audit added a fresh seed 2 the fixer did not use). Every previously
  confident-wrong case (`gain-diff`) now `unresolved`; offset-0 controls all
  `no-difference`; `depth_ratio ≈ 1.01` (confirms gate 2 is blind, gate 4b is the catcher).
- **No over-abstention (yes).** `pytest tests/inference/test_differential.py -m slow -q` →
  **11 passed** (404 s): genuine ×1.4–×4.0 ceiling & gain differences STILL resolve
  (`off_shift ≤ 1.96`), depth-confound still abstains, no-difference / threshold /
  underpowered / fail-safe controls intact, + the 3 new decoy configs. Gate 4b sits after
  gates 3/4, so it only fires when a positive `*-diff` would be emitted — cannot
  over-abstain on genuine `no-difference`.
- **Frozen core untouched (yes).** `git show --stat b562da9`: only
  `src/nudge/inference/differential.py` under `src/`.
- **Honesty framing accurate — not an overclaim.** CLOSED-for-inflating /
  BOUNDED-for-deflating stated consistently across `NUDGE-LIM-016`, FINDINGS §P1, the
  Mechanism Card, the module docstring; the deflating residual is disclosed, not hidden;
  `off_shift` correctly relabelled "one-sided load-bearing" (zero stale "non-load-bearing"
  in src). The 2.5 threshold is justified by the measured 1.96↔2.99 gap.
- **Full gate — raw:** ruff All passed · pyright 0 · 5 doc checkers OK ·
  check_hardening_append_only OK · `pytest -q` **254 passed, 5 skipped, 1 xfailed**.

## Branch (audit worktree — nothing merged by the audit)

- Branch: `worktree-agent-a3802d5cd637c0b74` (no `src/` edits).
