# 000000010 · uq-fixer · P4 (differential MULTIPLICATIVE perturbed-scale confound — LIM-016)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P4 fix (commit f5d0b870b56f008480290fb194258f360638e5d7)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (re-scan) → P2 → … until HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated (based on shared HEAD `b562da9`,
  which contains the P1 gate 4b — confirmed present before starting).
- **Target:** **P4** — `nudge.inference.differential` multiplicative perturbed-only scale
  → confident `ceiling-diff` (`NUDGE-METHOD-010`, `NUDGE-LIM-016`). Found by the P1-fix
  red-team re-scan (`runs/000000009`).

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped path, 2 seeds): a multiplicative factor `c` on ONE
  context's PERTURBED cells only (control clean), `mechanism="none"` → **9 confident-wrong
  `ceiling-diff`** across inflating (c=1.5/2/2.4) AND deflating (c=0.5/0.7); off_shift (gate
  4b) silent (<2.5) in 8/9; factor-1 → `no-difference`.
- **Measured root cause + separator:** a multiplicative `c` dilates the whole perturbed
  distribution about zero — including the OFF-cluster SPREAD — while a genuine `v_max`
  difference moves only the ON mode. Statistic: `off_scale` = MAD of below-median-activity
  cells (perturbed ÷ its own control), **RAW / un-clipped** (an initial clip — copied from
  the additive `off_shift` — collapsed the near-zero OFF cluster to a zero-spike, MAD→0/nan;
  removing it matched the validated sweep). Measured (both basal regimes, 3 seeds): genuine
  ceiling ×1.4–4 → **0.98–1.18**; inflating confound c≥1.5 → **1.43–2.59** (clean gap
  [1.18, 1.43]); deflating confound → **0.48–0.75**; genuine ceiling reduction ×0.5 →
  **0.61–0.69** (overlaps deflating).
- **Fix (additive; `src/nudge/inference/differential.py` ONLY — frozen core untouched):**
  a new **ceiling-scoped** classifier **gate 4c** — abstain before a `ceiling-diff` when
  either context's `off_scale` leaves the band **[0.80, 1.30]** (1.30 = midpoint of the
  measured inflation gap; 0.80 = catch threshold for the demonstrated deflating confound).
  Ceiling-scoped (`best_diff == "vmax"` only) because a global scale is degenerate with
  `v_max` specifically — genuine gain/threshold reshape the distribution and are untouched
  (zero over-abstention there). Added `_off_mode_scale_ratio` + `off_scale_a/off_scale_b`.
- **Status:** **INFLATION CLOSED** (clean measured gap); **DEFLATION BOUNDED** — a genuine
  strong ceiling *reduction* collapses the switch toward monostable and shrinks the OFF
  cluster into the same band as a deflating scale → indistinguishable, so both abstain. The
  honest price: a strong genuine ceiling reduction is no longer resolved (locked as a
  **strict-xfail**, honestly named). A per-context multiplicative scale without an
  independent depth anchor is fundamentally degenerate with a ceiling change.
- **Honesty record:** `NUDGE-LIM-016` sharpened (schema-valid) to name the multiplicative
  channel (inflating CLOSED / deflating BOUNDED); `scripts/vv/FINDINGS.md` §P4 (measured
  before/after); module docstring, Mechanism Card, STATE, CHANGELOG, README updated so
  nothing implies differential's perturbed-side confounds are all closed.
- **Regression lock (`tests/inference/test_differential.py`):**
  `test_decoy_multiplicative_perturbed_scale_abstains` (8 cases, inflating+deflating),
  factor-1 positive control, `test_genuine_ceiling_inflation_still_resolves_past_gate_4c`
  (×2.0), strict-xfail `test_genuine_ceiling_reduction_is_sacrificed_to_the_deflation_bound`,
  + 4 fast gate-4c unit tests.

## Branch + commit (NOT merged by the fixer)

- Branch: `worktree-agent-a4a6c754c6cd448f9` · Commit:
  `f5d0b870b56f008480290fb194258f360638e5d7` (+246/−13 in differential.py).

## Gate (fixer-reported; independently re-checked by audit, run 000000011)

repro 0 confident-wrong (2 seeds, both directions) · differential slow suite 22 passed + 1
xfailed (the deflation-bound lock) · ruff · pyright 0 · 5 checkers · `pytest -q` 258 passed.
No OOM (all heavy jobs sequential).
