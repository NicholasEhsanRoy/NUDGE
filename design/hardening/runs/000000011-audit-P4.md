# 000000011 · audit · P4 (differential multiplicative confound — LIM-016) → PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + MERGE the P4 fix (audit PASSED)
then  →  nudge-red-team (re-scan for P4-fix regressions + new holes)
then  →  nudge-uq-fixer on P2 (multi_reporter batch confound, LIM-014 — same class)
```

## Role / target

- **Role:** `nudge-audit` (role 4), worktree-isolated, independent of the fixer.
- **Target:** the P4 fix at commit `f5d0b870b56f008480290fb194258f360638e5d7` (its history
  contains the P1 gate 4b — both gates verified present).

## Verdict: **AUDIT: PASS**

Re-run from scratch (clean fast-forward merge; `uv sync`; all heavy jobs SEQUENTIAL,
OOM-aware).

- **Hole gone — both directions (yes).** `differential_multiplicative_confound.py 2`
  (on-disk `FACTORS=[1.5,2.0,2.4,0.7,0.5]`) → **HOLES: 0**; every confound factor
  (inflating + deflating) → `unresolved` (the fit still confidently picks vmax, dBIC 255–5893
  — gate 4c is what converts it to an abstention); factor-1 → `no-difference`. (Audit noted
  its Read/cat tools served a STALE pre-merge cache; it verified real on-disk content via
  sed/grep + execution — the deflating factors are genuinely present, FINDINGS §P4 accurate.)
- **No over-abstention / the sacrifice is NARROW & honest (the key P4 scrutiny) (yes).**
  Differential slow suite → **22 passed, 1 xfailed** (654 s): genuine ceiling ×1.4 & ×2.0
  resolve `ceiling-diff` (pass gate 4c); gain resolves; depth-confound abstains; the additive
  P1 confound still caught (no gate-4b regression); the strict-xfail bound-lock xfails as
  documented. Audit's OWN probe (seeds 1,3): a MILD ceiling ×0.8 → off_scale ≈ 1.02–1.12
  (INSIDE the band) → gate 4c does NOT fire (not sacrificed); only ×0.625 → off_scale
  ≈ 0.62–0.65 (< 0.80) abstains. So the deflation sacrifice is genuinely narrow (only
  reductions strong enough to deflate off_scale < 0.80) — **an acceptable, measured, honest
  trade-off, not a capability-killer.**
- **Honesty record accurate (yes).** LIM-016 names the multiplicative channel (inflating
  CLOSED / deflating BOUNDED); band [0.80,1.30] justified by FINDINGS §P4 (inflation gap
  1.18↔1.43, midpoint 1.30; 0.80 honestly stated as a "catch threshold," not a clean
  separator); the asymmetry stated loudly in LIM-016 / FINDINGS / the xfail reason.
- **Frozen core untouched (yes).** `git diff --stat 3fafbda..f5d0b87 -- src/` = only
  `differential.py` (+246/−13). No `fit.py`, no `core/`.
- **Full gate — raw:** ruff All passed · pyright 0 · check_anomalies/citations/impl_mapping/
  mechanism_cards/hardening_append_only OK · `pytest -q` **258 passed, 5 skipped, 1 xfailed**.

## Out-of-P4-scope observation (flagged, NOT a blocker)

A genuine MILD ceiling reduction ×0.8 at seed 1 was called `gain-diff` — a **pre-existing**
gain⇄ceiling-reduction degeneracy, unaffected by P4 (gate 4c is ceiling-scoped and was not
entered; `best_diff` was `n`). Not part of the P4 claim → does not invalidate the fix. A
candidate future red-team target (genuine-reduction mis-attribution), recorded for the trail.

## Branch (audit worktree — nothing merged by the audit)

- Branch: `worktree-agent-aa19b1a13abfdb773` (no `src/` edits).
