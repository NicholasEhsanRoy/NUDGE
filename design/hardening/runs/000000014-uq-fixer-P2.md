# 000000014 · uq-fixer · P2 (multi_reporter per-condition batch-scale confound — LIM-014)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P2 fix (commit b870354b8a83159ba152241a0141729b1dd247b0)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (FINAL FULL sweep) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated (based off `3fafbda`;
  `multi_reporter.py` unchanged since then).
- **Target:** **P2** — `nudge.inference.multi_reporter.attribute_multi_reporter`
  (`NUDGE-METHOD-008`, `NUDGE-LIM-014`). Red-team HOLE 2 (`design/FAILSAFE_REDTEAM_3.md`).
  The LAST open problem in the queue.

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped path, 2 seeds × 3 factors): a per-condition batch
  factor `c` on EVERY perturbed reporter (control clean), `mechanism="none"` (truth =
  no-effect) → **6/6 confident `ceiling`**; `A_perturbed/A_wt = c` to 3 digits; CI excludes
  0; knob_margin 130–1002, effect_margin 174–1712 (all ≫ thresholds). Positive controls
  (c=1.0) → `no-effect`.
- **Measured root cause + separation:** the discriminator is the **OFF baseline / floor**
  (dose→0). A genuine ceiling scales only `gain·A·f` → each reporter's floor unchanged; a
  batch scales the whole perturbed signal → every floor rescaled by `c`. Statistic
  `off_on_coupling = log(median perturbed/control OFF baseline) / log(A)` — ≈0 (ceiling),
  ≈1 (batch). Sweep (3 seeds): batch → +0.67…+1.01; genuine ceiling → −0.04…+0.11. At
  **floor = 0**, both ≈1.0 (pure ON-leakage, genuinely inseparable) — detected by
  `floor_measurability` (≈0.96 realistic, 0.52–0.70 tiny, ≤0.18 at floor 0).
- **Fix (additive; `src/nudge/inference/multi_reporter.py` ONLY — frozen core untouched):**
  `_floor_consistency(...)` computes `floor_measurability` / `floor_ratio` /
  `off_on_coupling` (+3 `MultiReporterFit` fields); a new **ceiling-scoped gate** in
  `classify_multi_reporter` (`min_floor_measurability=0.6`, `max_off_on_coupling=0.5`) —
  before a `ceiling` call, abstain if floors unmeasurable (BOUND) or the OFF baseline moved
  with the ON scale (batch). `0.5` = the physical midpoint (floor-fixed=0 ↔ floor-fully-
  rescaled=1); the measurability floor read off the sweep — not tuned.
- **Status:** **CLOSED (measurable floors)** — 0 confident-wrong; **BOUNDED/LOCKED
  (near-zero floors)** — an over-abstention on floor≈0 panels (batch ≡ ceiling without an
  independent depth anchor), never a confident-wrong.
- **Honesty record:** `NUDGE-LIM-014` sharpened (schema-valid) with the batch-scale channel
  + the near-zero-floor residual bound; `scripts/vv/FINDINGS.md` §P2 (measured before/after
  + separation table); module docstring corrected (it had implied the control-only
  consistency guard covers a per-condition scale), STATE, CHANGELOG, README, Mechanism Card.
- **Regression lock (`tests/inference/test_multi_reporter.py`):**
  `test_batch_scale_is_not_confidently_a_ceiling` (9 params),
  `test_batch_scale_at_realistic_floors_is_not_a_ceiling`,
  `test_genuine_ceiling_resolves_with_off_baseline_fixed`,
  `test_floorless_ceiling_abstains_the_documented_bound`, strict-xfail
  `test_floorless_genuine_ceiling_cannot_be_resolved_bound`.

## Branch + commit (NOT merged by the fixer)

- Branch: `worktree-agent-ad0267c9c7a2eeb76` · Commit:
  `b870354b8a83159ba152241a0141729b1dd247b0` (multi_reporter.py +160/−12).

## Gate (fixer-reported; independently re-checked by audit, run 000000015)

repro HOLES: 0 (2 seeds) · genuine ceiling ×3 resolves 3/3 · test_multi_reporter.py 28
passed + 1 xfailed · ruff · pyright 0 · 5 checkers · `pytest -q` 263 passed / 5 skipped /
2 xfailed. `multi_reporter` repro is light (scipy least_squares + small bootstrap, ~11 s).
