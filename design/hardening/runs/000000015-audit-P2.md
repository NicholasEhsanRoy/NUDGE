# 000000015 · audit · P2 (multi_reporter batch-scale confound — LIM-014) → PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + MERGE the P2 fix (audit PASSED)
then  →  nudge-red-team (FINAL FULL sweep — all of P3/P1/P4/P2 fixed)
STOP when nudge-red-team reports HOLES_FOUND: 0.
```

## Role / target

- **Role:** `nudge-audit` (role 4), worktree-isolated, independent of the fixer.
- **Target:** the P2 fix at commit `b870354b8a83159ba152241a0141729b1dd247b0`.

## Verdict: **AUDIT: PASS**

Re-run from scratch (clean fast-forward merge; `uv sync`).

- **Hole gone (yes).** `multi_reporter_batch_confound.py 3` (seeds 0,1,**2** — audit added a
  fresh seed) → **HOLES: 0**; every batch factor {0.5,0.6,0.75} → `unresolved` (the fit's
  winner is still `ceiling` internally, the new gate abstains); factor-1.0 → `no-effect`.
  Batch at REALISTIC floors (0.2,0.6): abstains 6/6 (seeds 0,1,2 × c∈{0.5,0.75}),
  `off_on_coupling` **0.672–0.778** > 0.5, floor_meas ≈ 0.98.
- **No over-abstention — the key P2 scrutiny (yes).** Genuine `ceiling` (factor 3, default
  floors) resolves **`ceiling` 4/4** (seeds 0,1,2,**7**), `off_on_coupling` −0.014…+0.013,
  floor_meas ≈ 0.96–0.98; genuine `threshold` 3/3, `gain` 3/3 resolve.
  `pytest tests/inference/test_multi_reporter.py` → **28 passed, 1 xfailed** (53 s).
- **Near-zero-floor BOUND — acceptable honest trade-off.** On a floor≈0 panel the perturbed
  OFF doses are pure ON-leakage `gain·A·f`, which a batch and a genuine ceiling scale
  *identically* — mathematically inseparable without an external depth anchor NUDGE doesn't
  ingest. The fix over-abstains (never a confident-wrong), is **narrow** (a genuine ceiling
  with any measurable floor still resolves), and is documented loudly (strict-xfail + LIM +
  FINDINGS labelled BOUND). Correct fail-safe posture.
- **Honesty record accurate.** LIM-014 names the batch-scale channel + near-zero-floor bound
  (severity major); module docstring no longer implies the control-only guard covers a
  per-condition scale; thresholds grounded in the measured sweep (genuine 0.00–0.10 vs batch
  0.67–1.00; 0.5 = physical midpoint). All re-measured numbers reproduce.
- **Frozen core untouched (yes).** `git show --stat b870354 -- src/`: only `multi_reporter.py`.
- **Full gate — raw:** ruff All passed · pyright 0 · check_anomalies/citations/impl_mapping/
  mechanism_cards/hardening_append_only OK · `pytest -q` **263 passed, 5 skipped, 2 xfailed**.

## Branch (audit worktree — nothing merged by the audit)

- Branch: `worktree-agent-aa5c2ebad1d6975f7` (no `src/` edits).
