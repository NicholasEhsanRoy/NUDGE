# 000000018 · audit · P5 (differential small multiplicative confound — LIM-016) → **FAIL**

*Immutable run record (write-once). This is currently the highest-numbered record, so its
`NEXT →` block below is the AUTHORITATIVE resume pointer (`../LEDGER.md` mirrors it).*

## ▶ NEXT →

```
NEXT  →  nudge-uq-fixer  on  P5 AGAIN (AUDIT FAILED — the fix does NOT close the hole)
then  →  nudge-audit → orchestrator merge → nudge-red-team ; STOP at HOLES_FOUND: 0
NOTE:  the failed fix (branch worktree-agent-a16011ffecc46d5b9 @ 779fc3a) is NOT merged.
```

## Role / target

- **Role:** `nudge-audit` (role 4), worktree-isolated, independent of the fixer.
- **Target:** the P5 fix at `779fc3afc45ed8d00ba33bd80ae62ea213d9fbcb` (de-scoped gate 4c,
  cut 1.30→1.20). **NOT merged.**

## Verdict: **AUDIT: FAIL** — the demonstrated hole is NOT closed; the fix's core claim is false

1. **Demonstrated hole still open (the decisive FAIL).** The repro's OWN DEFAULT is **4
   seeds**; the fixer re-validated with only `...gain_hole.py 2`. At 4 seeds:
   `differential_small_mult_gain_hole.py 4` → **HOLES: 3 across seeds 2–3** (truth =
   no-difference): `seed2 c=1.15 ceiling-diff off_scale=1.131`; `seed2 c=1.20 ceiling-diff
   off_scale=1.180`; `seed3 c=1.15 gain-diff off_scale=1.138`. All three `off_scale ≤ 1.180 <
   1.20` cut → gate 4c never fires. The fix generalized only to the two seeds it was checked on.
2. **RESIDUAL #1 — the cut is TOO LOOSE (not the thin degeneracy the fix documents).**
   Independent 10-seed direct-off_scale measurement, RED-TEAM regime (default
   `ras_switch_1node`, basal=0.05 — the regime the repro runs in):
   - genuine ceiling ×1.4/×2/×4 → off_scale **max 1.104**
   - the three confident-wrong holes at off_scale **1.131 / 1.138 / 1.180** all sit **ABOVE**
     the genuine-ceiling max (1.104) yet BELOW the 1.20 cut → **SEPARABLE from genuine ceiling
     → the cut is simply too loose.** A measured cut ~1.11–1.12 would catch all three while
     sparing genuine ceiling. (TEST regime basal=0.2: genuine max 1.184, confound low tail
     overlaps → partly irreducible there; but the shipped fix emits confident-wrong on the
     demonstrated hole regardless.) The genuine-max is **regime-dependent** (1.104 RT vs 1.184
     TEST) — a single global 1.20 justified only by the TEST max is invalid for the RT regime.
3. **Honesty OVERCLAIMS (independently falsified).** The fix's central measured claim does not
   reproduce: the "measured gap **[1.184, 1.231]**" / "every confident confound drove off_scale
   **≥ 1.231**" (FINDINGS §P5, LIM-016) is FALSE — confident `ceiling-diff`/`gain-diff` measured
   at off_scale **1.131, 1.138, 1.180** (all < 1.231) in that very regime. The documented
   residual ("a very small confound in the sliver (1.184, 1.20]… one seed") **materially
   understates** a wide confident-wrong band down to off_scale ~1.13 across multiple seeds. The
   "0 across 2 seeds" re-validation is cherry-picked / under-powered.
4. **RESIDUAL #2 (gain-reduction sacrifice) — honest but moot.** The strict-xfail lock is
   present and the degeneracy real (gain ×0.55 → off_scale ~1.249, overlaps the confound) —
   documented honestly; not the failing point.
5. **Frozen core: yes** (`git diff --stat f0e0202..779fc3a -- src/` = only `differential.py`).
   Full gate not run — the FAIL is decided at step 1 (the demonstrated hole must return 0).

## What the fixer MUST change (from the audit)

1. **Tighten the too-loose 1.20 cut** to a MEASURED margin above the genuine-increase off_scale
   max, and handle the **regime dependence** (1.104 RT vs 1.184 TEST) honestly — a global cut
   justified only by the TEST regime is invalid for the RT regime.
2. **Abstain (not resolve) across the confounded band.** The remainder (confound low tail
   ~1.04–1.11 overlapping genuine ceiling for small c) is an irreducible degeneracy → per
   abstain-over-confident-wrong, ABSTAIN there (accepting over-abstention where it overlaps
   genuine increases), OR document small perturbed-only multiplicative scales as an unguardable
   confound. It may NOT ship confident `ceiling-diff`/`gain-diff` on a no-difference confound.
3. **Retract the falsified numbers** everywhere (docstring, `_OFF_SCALE_INFLATION_MAX` comment,
   FINDINGS §P5, LIM-016): the "gap [1.184, 1.231]" / "floor ≥1.231" / "thin sliver at one
   seed" → replace with the actual wide band (down to ~1.13) + correct genuine maxima (1.104 RT
   / 1.184 TEST).
4. **Re-validate with ≥4 seeds** (the script default), never 2.

## Branch (audit worktree; nothing merged)

- Audit branch: `worktree-agent-afccb9490486c6b18` (no `src/` edits).
- Failed fix (NOT merged): `worktree-agent-a16011ffecc46d5b9` @ `779fc3a`.
