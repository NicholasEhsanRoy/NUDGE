# 000000019 · uq-fixer · P5 (differential small multiplicative confound — LIM-016), ATTEMPT 2

*Immutable run record (write-once). Supersedes the FAILED attempt (`779fc3a`, audited in
`runs/000000018`). The live pointer is the newest record's `NEXT →` block + `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P5 re-fix (commit 1904b46, HEAD of worktree-agent-a7c7ce9742440090b)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (FINAL FULL sweep) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated. **Second attempt** after the first
  P5 fix FAILED audit (under-powered 2-seed validation + a falsified "gap [1.184,1.231]" claim
  + a too-loose 1.20 cut).
- **Target:** **P5** — `nudge.inference.differential` small multiplicative perturbed-only scale
  → confident `gain-diff`/`ceiling-diff` (`NUDGE-LIM-016`).

## What it did (fix claim, condensed)

- **Independently reproduced at the DEFAULT 4 seeds** (the count the first fix under-powered):
  RT `differential_small_mult_gain_hole.py 4` → **HOLES: 3** (matching `runs/000000018`).
- **Measured root cause (the first fix's premise falsified):** `off_scale` does NOT separate
  the small confound from a genuine ceiling in EITHER regime — confident-wrong confounds land
  at off_scale 1.08–1.28, inside the genuine range. `off_shift` fails too. Physically: a
  uniform scale corrupts the μ_off/μ_on mode ratio, faking gain/ceiling-diff **precisely when
  the OFF mode is near the noise floor** (scaling μ_off≈0 by c leaves it ≈0). (Also found:
  `calibrate_from_wt`'s `obs_sd` is erratic 0.01–2.44, so a scale-invariant metric is used.)
- **Fix (additive; `differential.py` ONLY — frozen core untouched):** gate 4c reworked, scoped
  to the `{n, v_max}` channels a scale is BIC-assigned to (was ceiling-only):
  1. **Resolvability gate** — abstain on gain-diff AND ceiling-diff when either context's OFF
     mode isn't resolvably above zero: `off_resolvability` = (OFF-cluster level ÷ clean-control
     spread) `< _OFF_RESOLVABILITY_MIN = 0.25`. Measured **RT ≤ 0.136 vs TEST ≥ 0.374** (a
     clean gap; floor = midpoint). Catches the near-zero-basal regime.
  2. **Ceiling-magnitude gate** — in the resolvable regime, resolve `ceiling-diff` only when
     `|log2 v_max ratio| ≥ _CEILING_RESOLVE_MAG_MIN = 0.60`. Escaping confounds (off_scale ≤
     1.30) fake only `|log2| ≤ ~0.50`; genuine ×1.6→0.68, ×2→1.0 clear it.
  3. The P4 gross-scale band `[0.80, 1.30]`, now on the gain channel too.
- **Status:** **BOUNDED, not closed (honest).** A perturbed-only multiplicative scale cannot be
  separated from a *small* ceiling/gain difference from a single snapshot without an independent
  depth anchor. Over-abstention price (documented): (a) a small genuine ceiling (<~×1.5) abstains
  (strict-xfail); (b) the whole ceiling/gain channel abstains in a near-zero-basal regime (a
  genuine gain difference doesn't resolve there anyway). Genuine LARGE ceilings, resolvable
  gain, threshold, no-difference preserved.
- **Honesty record:** `NUDGE-LIM-016` re-sharpened (retracts the first attempt's clean-gap /
  "INFLATION CLOSED" claim, states the resolvability+magnitude BOUND); `scripts/vv/FINDINGS.md`
  §P5 rewritten with the actual per-regime overlap table; a `⚠️ SUPERSEDED IN PART BY §P5`
  banner on §P4; overclaim corrected in the module docstring, STATE, CHANGELOG, README,
  Mechanism Card, notebook. Revised the falsified locked test →
  `test_classify_gross_scale_guard_now_covers_the_gain_channel` (asserts abstain); strict-xfail
  `test_small_genuine_ceiling_is_sacrificed_to_the_P5_bound`; decoy
  `test_decoy_small_multiplicative_scale_abstains` (both regimes).

## Re-validation (0 confident-wrong at 4 seeds, BOTH regimes)

- RT `...gain_hole.py 4` → **HOLES: 0 across 0/4** (was 3). TEST `...testregime.py 4` (NEW repro)
  → **HOLES: 0 across 0/4**. Positive controls: genuine ceiling ×2 → ceiling-diff, gain ×0.55 →
  gain-diff, no-difference → no-difference.

## Branch + commit (NOT merged by the fixer)

- Branch: `worktree-agent-a7c7ce9742440090b` · HEAD `1904b46` (fix `f6990c6` src · `b95798d`
  tests+docs · `1904b46` notebook; a merge `f08b12d` brought in the P1–P4 trail + origin/main).

## Gate (fixer-reported; independently re-checked by audit, run 000000020)

RT+TEST repros HOLES: 0 (4 seeds each) · differential slow suite 34 passed + 2 xfailed · ruff ·
pyright 0 · 5 checkers · fast `pytest -q` 277 passed / 6 skipped / 2 xfailed · notebook 0 errors.
