# 000000019 · uq-fixer · P5 (differential small multiplicative confound — LIM-016)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on the P5 fix (commit 416c17e)   [DONE → runs/000000020, PASS]
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  P6 audit → merge → nudge-red-team (re-scan) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), in worktree `/home/user/NUDGE-p5-worktree` (branch
  `claude/p5-uq-fix`, based on `c491c47`).
- **Target:** **P5** — `nudge.inference.differential.attribute_differential` (`NUDGE-METHOD-010`,
  `NUDGE-LIM-016`). Red-team round 5 hole (`runs/000000017`).

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped `attribute_differential`, `ras_switch_1node`, N=3000): a
  SMALL uniform multiplicative scale `c ∈ [1.15,1.25]` on ONE context's PERTURBED cells (control
  clean, truth=no-difference) → confident `gain-diff`/`ceiling-diff` slipping gate 4c (ceiling-
  scoped → silent on the `n` winner at small `c`; and `off_scale` in the `(1.18,1.30]` blind gap).
- **The fix — guard the identifiability CLASS, not the confound magnitude (gate 4d).** P1
  (additive), P4 (large mult), P5 (small mult) are ONE thing — a per-condition affine `y=s·x+o`
  on the perturbed side. Instead of a fourth per-magnitude band, gate 4d adds `(s,o)` as a FREE
  nuisance on the perturbed context and abstains before any positive `*-diff` unless the
  BIC-winning knob **earns** its parameter over a pure-affine null by ≥ `_NUISANCE_EARN_MARGIN=6.0`
  (profiled ΔBIC `earn`, min over both directions). The whole confound family is by construction
  inside the null's span → no `(s,o)` lets the bio knob earn. Runs only for a candidate-positive
  winner. Ported from the measured `_proto_nuisance` prototype (`design/PERTURBED_CONFOUND_STRATEGY.md`,
  the `design/STATE.md` §4 principle).
- **Measured separator:** confound family (P1/P4/P5) `earn` −7.5…−2.1 (all < 0); genuine gain
  ×0.55 +59…+96, genuine ceiling ×1.4/×2.0 +116…+616 (prototype magnitudes; shipped-path
  re-measure ≈ +90 for ×1.4 — see the P5-audit honesty note in FINDINGS §P5). Margin 6.0 sits in
  the plateau — a single margin on a GLOBAL fit statistic, not a per-confound band.
- **Status:** **CLOSED** for the UNIFORM per-condition affine class (P1/P4/P5), 0 confident-wrong,
  every positive control preserved. **BOUNDED (honest):** a NON-uniform above-median-only scale is
  observationally identical to a genuine ceiling → abstain (needs an inert-feature anchor); the
  gate-4c deflation ceiling-reduction sacrifice is unchanged. Corrects the falsified P4 "INFLATION
  CLOSED" wording (module docstring + gate-4c comment + FINDINGS §P4); revises the falsified locked
  test `test_classify_off_scale_guard_is_ceiling_scoped`.
- **Honesty record:** `NUDGE-LIM-016` sharpened (P5 channel + residual bounds); `FINDINGS §P5`
  (measured table); mechanism card / STATE / CHANGELOG / README corrected. Decoy
  `test_decoy_small_multiplicative_perturbed_scale_abstains` (strict lock).

## Operational note (relayed)

The fixer completed the fix + validation but **stalled before committing** — its pattern was to
launch a background repro (`nohup … &`) and yield, ending its turn with the work staged-not-
committed (twice). The orchestrator finished the last validation (post-fix repro `HOLES: 0`, fast
gate green, frozen core clean), removed the transient `*.log` files, and **committed the fixer's
complete staged work** as `416c17e` (the fixer's authored changes; no orchestrator edits to `src/`).

## Branch + commit

- Branch: `claude/p5-uq-fix` · Commit: **`416c17e`** (11 files: `differential.py` +service +tests
  + docs/known_limitations + FINDINGS + mechanism card + STATE + CHANGELOG + README + p5_measure.py).
- Frozen core (`fit.py`, `core/`) untouched (verified empty diff `c491c47..416c17e`).
