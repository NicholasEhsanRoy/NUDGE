# 000000020 · audit · P5 (differential small multiplicative confound — LIM-016) ATTEMPT 2 → PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block +
`../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + MERGE the P5 re-fix (audit PASSED)
then  →  nudge-red-team (FINAL FULL sweep — all holes P3/P1/P4/P2/P5 addressed)
STOP when nudge-red-team reports HOLES_FOUND: 0.
```

## Role / target

- **Role:** `nudge-audit` (role 4), worktree-isolated. Held to a HIGH bar (the first P5 fix
  passed-looking but was false — under-powered 2 seeds).
- **Target:** the P5 re-fix at `1904b46`.

## Verdict: **AUDIT: PASS**

Re-run everything from scratch (clean fast-forward merge; `uv sync`); ≥4 seeds + extra +
interior factors; all heavy jobs SEQUENTIAL.

- **Hole gone in BOTH regimes at ≥4 (+extra) seeds (yes).** RT `...gain_hole.py 6` (seeds 0–5)
  → **HOLES: 0 across 0/6** (the exact regime the FIRST fix failed at seeds 2–3 — now abstains
  via the resolvability gate). TEST `...testregime.py 5` (seeds 0–4) → **HOLES: 0 across 0/5**.
  **Interior-factor probe** (own script, beyond the repro's factors): TEST c∈{1.28,1.30,1.35,
  1.40}×3 + RT c∈{1.30,1.40}×2 → **INTERIOR HOLES: 0**. No c slips between the magnitude gate
  (abstains within-band) and the gross-scale gate (`|log2|≥0.60` ⇒ off_scale>1.30 ⇒ caught).
- **The two separators are genuine gaps, not knife-edges.** Resolvability (own re-impl, 6 seeds
  × 2 contexts): RT 0.000–0.136 (max 0.1357) vs TEST 0.374–0.653 (min 0.3736) — ~0.11 margin
  each side of 0.25. Ceiling-magnitude: escaping confounds reach `|log2|` at most **0.498**
  (c=1.35 probe; 0.477 at the repro factors) vs cut 0.60 vs genuine ×1.6→0.68 — ~0.10 margin
  (contrast the LIM-017 0.007 knife-edge).
- **Over-abstention narrow + documented (yes).** Differential slow suite → **34 passed, 2
  xfailed**. Genuine ×2 ceiling → `ceiling-diff`; genuine ×0.55 gain → `gain-diff`; threshold /
  no-difference intact. Sacrifices exactly the two strict-xfail locks (small genuine ceiling
  <~×1.5; genuine ceiling reduction) + the documented near-zero-basal channel — not broader.
- **Falsified numbers retracted / no overclaim (yes).** The first fix's "[1.184,1.231]" /
  "floor ≥1.231" / "thin sliver" appear ONLY as explicit retractions; §P4 carries a SUPERSEDED
  banner; LIM-016 states BOUNDED-not-closed with the actual numbers.
- **Frozen core (yes).** `git diff --stat 9510f28..HEAD -- src/` = only `differential.py`
  (+183/−63). `invert.py`/`multi_reporter.py` present, unchanged by P5.
- **Full gate:** ruff PASS · pyright 0 · 5 checkers OK · fast `pytest -q` **277 passed, 5
  skipped, 2 xfailed** · differential slow **34 passed, 2 xfailed**.

## Minor honesty nuance (advisory, NOT a FAIL — actioned by the orchestrator)

FINDINGS §P5 / LIM-016 stated the escaping-confound magnitude "≤ 0.48" (true at the repro
factors {1.15,1.20,1.25}), but at the wider `c=1.35` the audit additionally probed, it measured
**0.498** — the 0.60 cut still abstains it with ~0.10 margin (no positive control affected). The
orchestrator tightened the doc claim to "≤ ~0.50 (0.498 at c=1.35)" so the number matches the
measurement (`runs/000000021`).

## Branch (audit worktree — nothing merged)

- Branch: `worktree-agent-ae05be7d62d4ec65a` (no `src/` edits).
