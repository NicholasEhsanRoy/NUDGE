# 000000020 · audit · P5 fix → AUDIT: PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + merge P5 (audit PASS)   [DONE → runs/000000021]
then  →  P6 audit (runs/000000023) → orchestrator merge P6
then  →  nudge-red-team (re-scan: P5+P6 regression + cover unreached adjoint.ode_identifiability / OED multi-knob)
STOP when nudge-red-team reports HOLES_FOUND: 0 after a genuine FULL sweep.
```

## Role / target

- **Role:** `nudge-audit` (role 4), independent re-verification in `/home/user/NUDGE-p5-worktree`.
- **Target:** the P5 fix (`claude/p5-uq-fix`, `416c17e`). **Did not trust the fixer** — re-ran the
  hole with the fixer's seeds AND fresh independent seeds.

## Verdict — **AUDIT: PASS**

- **Hole abstains:** `differential_small_mult_gain_hole.py 2` → **HOLES: 0** (the three original
  confident-wrong cases → `no-difference`, incl. seed1 c=1.25 `off_scale=1.279` squarely in the
  `(1.18,1.30]` gap gate 4c cannot catch — gate 4d caught it). **Independent fresh seeds 5,7** →
  **HOLES: 0**; shipped `res.fit.earn` = −7.46/−6.88/−6.87/−7.52 (all < 0). 4 distinct seeds clean.
- **No over-abstention:** slow differential suite **29 passed, 1 xfailed** (0 failed). Positive-
  control locks pass (`test_recovers_ceiling_difference`, `test_genuine_ceiling_earns_over_the_affine_nuisance[1,2]`,
  `test_genuine_gain_earns_over_the_affine_nuisance`, `test_genuine_ceiling_inflation_still_resolves_past_gate_4c[1,2]`);
  P5 decoys + P1/P4 decoys pass. The 1 xfailed is the documented deflation-sacrifice strict-xfail
  (not a regression). Independent genuine ceiling ×1.4 resolves (earn +94.28/+89.68 ≫ 6).
- **Honesty record accurate:** confound `earn < 0` and genuine `earn ≫ 6` reproduce on the shipped
  path; margin 6.0 measured, wide headroom. P4 "INFLATION CLOSED" wording corrected in place; P5
  decoy is a strict lock; the falsified `test_classify_off_scale_guard_is_ceiling_scoped` was
  removed + replaced by gate-4d contract tests.
- **Minor, non-blocking note (acted on):** the FINDINGS §P5 *genuine*-earn magnitudes (ceiling ×1.4
  +116…+131) are prototype numbers (`p5_measure.py`, steps=150); the shipped-path re-measurement
  (steps=200) gives ~+90 — lower but still ≫ margin, nothing load-bearing. **Orchestrator corrected
  the FINDINGS §P5 label** additively at merge (`runs/000000021`) so the genuine magnitudes are no
  longer mislabeled "through the shipped path"; the safety-relevant confound-side numbers do
  reproduce shipped-path.
- **Frozen core untouched:** `git diff c491c47..416c17e -- fit.py core/` empty.
- **Full gate green:** ruff (all pass) · pyright `src` 0 errors · 5 checkers OK · `pytest -q` fast
  **301 passed / 5 skipped / 2 xfailed** · slow differential **29 passed / 1 xfailed**.

**Conclusion:** gate 4d closes the whole uniform per-condition affine class (P1/P4/P5) via a single
measured, threshold-free global goodness-of-fit test; the non-uniform residual is honestly BOUNDED.
