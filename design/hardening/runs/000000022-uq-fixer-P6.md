# 000000022 · uq-fixer · P6 (matrix-free sloppiness isolated-null mislabel — LIM-023)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on the P6 fix (commit 2d54b57)   [DONE → runs/000000023, PASS]
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (re-scan) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), in worktree `/home/user/NUDGE-p6-worktree` (branch
  `claude/p6-uq-fix`, based on the correct post-moat HEAD `95a950e`).
- **Target:** **P6** — `nudge.inference.sloppiness.sloppiness_diagnostic_matrixfree` /
  `analyze_model_matrixfree` (`NUDGE-LIM-023`). Red-team round 6 hole (`runs/000000018`).

## Dispatch note (a harness gotcha the orchestrator caught)

The FIRST P6-fixer dispatch used the harness `isolation: "worktree"`, which based the worktree off
**pre-moat `3fafbda`** — where `sloppiness.py` does not exist. The orchestrator detected this
(`git worktree list` showed base `3fafbda`; the matrix-free code was absent), **stopped** the
doomed agent, created a correct worktree off HEAD `95a950e` itself, and re-dispatched. Lesson: for
these fixers, pre-create the worktree off the feature HEAD rather than trusting harness isolation.

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped `analyze_model_matrixfree`, DEFAULT `method="auto"`): a
  structural null (two params entering only via their sum ⇒ a provable Fisher zero) → confident
  `well-constrained` (n_null=0), 6/6, x64 ON. Root cause: `_verified_smallest_eigsh` Rayleigh-checks
  eigenpair-*ness* not smallest-*ness*; `eigsh('SA')` misses the isolated zero → certifies wrong.
- **The fix (additive; `sloppiness.py` only — `fit.py`/`core/` frozen, verified untouched):**
  (1) `method="auto"` now DEFERS to the exact dense-via-matvec reconstruction (`_dense_spectrum_from_matvec`)
  up to **`dense_below=2048`** (raised from 256) — a MEASURED threshold: reconstruct+`eigh` ≈18 s /
  0.7 GB at n=2000, recovering the exact null at every size 256–4096, routing the whole realistic/
  default regime through the exact path. (2) Above that, an **inverse-iteration null probe**
  (`_smallest_eig_null_probe`, shift-invert `(FIM+εI)⁻¹` via CG, matrix-free) — inverse iteration
  amplifies the smallest eigenvalue so it CATCHES the isolated null `eigsh` misses (measured probe RQ
  ≪ the rank floor `rank_rtol²·λ_max`; 0 false nulls on well-conditioned + sloppy controls). Used
  **one-sided**: a low RQ proves a null → `unidentifiable`; when no null is found the path ABSTAINS
  (`unidentifiable`, "cannot certify the smallest eigenvalue"). Trace-completeness (`tr FIM = Σλ`)
  was measured and REJECTED (a missed zero changes the trace by 0 → blind).
- **Status:** **CLOSED** — the isolated-structural-null mislabel: 0/6 confident-wrong via default
  `auto` + explicit `iterative`; positive controls (`well-constrained`, `sloppy-but-predictive`)
  still resolve via the dense path; dense==auto agreement preserved. **BOUNDED (locked):** above
  `dense_below`, a genuine well-constrained model with no isolated null OVER-ABSTAINS
  (`unidentifiable`) — the fail-safe direction, NEVER confident-wrong; exact verdict recoverable via
  `method="dense"`. Locked by a strict-xfail decoy.
- **Honesty record:** `NUDGE-LIM-023` sharpened (severity → major; the eigenpair-ness≠smallest-ness
  gap; CLOSED/BOUNDED); `FINDINGS §P6` (measured before/after + threshold + probe tables);
  overclaiming "iterative path is fail-safe" wording corrected in `sloppiness.py` docstrings +
  `design/STATE.md` + `CHANGELOG.md`.
- **New tests:** `tests/inference/test_sloppiness_p6_structural_null.py` — the structural-null →
  `unidentifiable` regression (default `auto` + explicit iterative, 3 seeds), the probe-catches-null
  slow test (n=3000), positive-control/equivalence tests, and the strict-xfail decoy
  `test_p6_decoy_huge_wellconditioned_cannot_be_certified_iterative`.

## Branch + commit

- Branch: `claude/p6-uq-fix` · Commit: **`2d54b57`** (6 files: `sloppiness.py` + the new test +
  known_limitations + FINDINGS + STATE + CHANGELOG).
- Frozen core (`fit.py`, `core/`) untouched (verified empty diff `95a950e..2d54b57`).
