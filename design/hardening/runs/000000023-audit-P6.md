# 000000023 · audit · P6 fix → AUDIT: PASS

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  orchestrator: independently verify + merge P6 (audit PASS)   [DONE → runs/000000024]
then  →  nudge-red-team (re-scan): P5+P6 regression + the two UNREACHED moat surfaces
         (adjoint.ode_identifiability through a real ODE; OED multi-knob / end-to-end)
STOP when nudge-red-team reports HOLES_FOUND: 0 after a genuine FULL sweep.
```

## Role / target

- **Role:** `nudge-audit` (role 4), independent re-verification in `/home/user/NUDGE-p6-worktree`
  (+ a throwaway detached worktree at base `95a950e` for the fast-lane count reconciliation, removed).
- **Target:** the P6 fix (`claude/p6-uq-fix`, `2d54b57`). Trusted no fixer log.

## Verdict — **AUDIT: PASS**

- **Hole closed:** `sloppiness_matrixfree_iterative_mislabel.py` → `HOLES: 0 / 6`, **exit 1**; all 6
  cells (`auto` default n=300 seeds 0/1/2; explicit iterative n=40 seeds 0/1/2) → `unidentifiable
  (n_null=1)`. Independent seeds **7,13,42**: `auto` n=300 + explicit iterative n=300 (forces the
  Krylov path) + iterative n=40 all → `unidentifiable` (lam_min ~1e-10…1e-11). Slow
  `test_p6_iterative_regime_probe_catches_null_above_dense_below` (n=3000, genuine iterative regime)
  PASSED — the probe catches the isolated null.
- **No confident-wrong remains; BOUNDED residual is a GENUINE fail-safe abstention.** The iterative
  smallest-end branch can only produce `unidentifiable` (probe-injected null) OR
  `smallest_certified=False` → abstention; a positive label requires the exact dense/full-spectrum
  path. Confirmed at n=2100>2048 (genuinely well-conditioned): `auto` → `unidentifiable`
  ("MATRIX-FREE ABSTENTION"), `method="dense"` → `well-constrained` (exact verdict on demand).
  Strict-xfail decoy `test_p6_decoy_huge_wellconditioned_cannot_be_certified_iterative` **XFAILED**
  (not xpass).
- **No over-abstention on the affordable path:** well-conditioned n=300 → `well-constrained`; sloppy
  sum-of-exp → `sloppy-but-predictive` (dense==auto). `test_sloppiness_p6_structural_null.py` +
  `test_sloppiness_matrixfree.py` (fast+slow): **21 passed, 1 xfailed**.
- **Threshold grounded / probe one-sided:** dense reconstruct+`eigh` at n=2000 = **11.8 s / 0.75 GB**,
  null recovered → `dense_below=2048` justified. Probe: `found_null=True`, RQ ≤ floor on every P6
  model (seeds 0/7/13/42 × n∈{40,300}); `found_null=False`, RQ≈λ_min≈216 ≫ floor on the
  well-conditioned control (the one-sided justification).
- **Frozen core untouched:** `git diff 95a950e..2d54b57 -- fit.py core/` empty (only `sloppiness.py`
  changed under `src/`).
- **Full gate green:** ruff pass; pyright **pinned 1.1.411 → 0 errors** (local 1.1.408 shows the 2
  documented pre-existing `bridge.py:180-181` errors on the untouched `bridge.py`); 5 checkers OK
  (`NUDGE-LIM-023` now severity **major**, schema-valid). **Fast-lane reconciliation (same env, both
  commits):** base `95a950e` = 283 passed / 6 skipped / 90 deselected / 2 xfailed; fix `2d54b57` =
  292 passed / 6 skipped / 92 deselected / 2 xfailed — delta = **+9 passed** (new fast P6 tests) +2
  deselected (new slow P6 tests); **zero pre-existing test failed/skipped/deselected**. The
  "296→292" apparent drop is a cross-environment artifact (the 296 baseline env had optional deps
  present); within one env the fix ADDS 9 tests.

## Honesty discrepancy flagged (non-blocking; corrected at merge)

The inverse-iteration probe Rayleigh-quotient figure recorded as **"~1e-19"** (`FINDINGS §P6` table
"5.6e-19/1.0e-19", `NUDGE-LIM-023`, `CHANGELOG`) does **NOT reproduce** — it is below float64's
cancellation floor `λ_max·ε ≈ 6e-12` (λ_max~2.6e4) and internally inconsistent with the finding's own
reproducing prose (`λ_min ≈ 1e-12`). **Decorative** — the safety-relevant claim it supports (RQ ≤ the
rank floor → null caught) reproduces robustly across 9+ seed·size cells — so it changes no verdict,
threshold, or the fail-safe guarantee. The orchestrator **independently re-measured** (seeds 0–2 ×
n∈{40,300}: RQ ~1.1e-12 … ~1.8e-10, ≪ the rank floor ~3e-10) and **corrected all three docs**
(`runs/000000024`, commit `ed3c381`). The audit did not FAIL on it because the substance (hole
closed, no possible confident-wrong, positive controls resolve, BOUNDED honestly locked, zero
regression) is sound and the finding already stated the correct order elsewhere.
