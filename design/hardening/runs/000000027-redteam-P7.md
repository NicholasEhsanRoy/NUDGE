# 000000027 · redteam · P7 (lyapunov multi-point breaker — gain resolved as threshold)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-uq-fixer  on  P7 (attribute_lyapunov_multi, NUDGE-LIM-025)   [DONE → runs/000000028]
then  →  nudge-audit: independently re-verify the P7 fix
then  →  orchestrator: verify + merge iff AUDIT PASS → nudge-red-team (re-scan) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** red-team finding (surfaced by the main loop; **re-opens** the hardening loop that
  `runs/000000025` had STOPped at HOLES_FOUND: 0). Reproduced from scratch by the uq-fixer.
- **Target:** **P7** — `nudge.inference.lyapunov.attribute_lyapunov_multi` (M3, the
  gain-vs-threshold breaker). New `NUDGE-LIM-025`.

## The hole (verified confident-wrong, through the shipped API)

- Setup: mutual-repression toggle `Circuit([A basal0.05, B basal bB], [hill_repression K1 n4
  vmax2 ×2])`, LNA-mixture ground truth (`sample_lna_mixture`), `SCALE=20`, `OBS_SD=0.5`, two
  operating points `bB ∈ {0.05, 0.30}`, 3000 cells each, `calibrate_from_wt`.
- **gain n=1.5** (true knob = GAIN) → `attribute_lyapunov_multi` resolves a CONFIDENT WRONG
  **`threshold`**, NLL gap **≈1.70 ≫ resolve_margin 0.03**, 2/2 seeds. (Positive controls hold:
  threshold K=2.0 → `threshold`; gain n=2.4 → `unresolved`, gap ≈0.005.)
- Secondary crash: a monostable-baseline operating point (`bB=0.50`, toggle gone monostable)
  raised `ValueError: WT circuit must present 2 stable modes to fit` in `calibrate_from_wt` — an
  ungraceful crash.

## Measured root cause

- At n=1.5 the perturbation is threshold-DOMINATED in the LNA moments (flattening the Hill curve
  shifts the effective EC50) AND the perturbed condition slides through the fold: **monostable at
  bB=0.30** (`bifurcation_proximity` → None), **at the fold at bB=0.05** (prox ≈0.539). So the
  second operating point carries no independent gain-vs-threshold information — the degeneracy is
  NOT broken — yet the pure NLL-gap test resolves anyway. The existing near-fold guards
  (`NUDGE-LIM-017` graded down-weighting + best-buffered-pair corroboration) inspect the
  WT/CONTROL circuit and are blind to a PERTURBED condition at the fold — the systemic pattern.

## Repro

- `scripts/redteam/lyapunov_multi_gain_threshold_hole.py` (added in the P7 fix commit).
  `uv run python scripts/redteam/lyapunov_multi_gain_threshold_hole.py 2` → reports HOLES (before
  the fix: the n=1.5 confident-wrong + the monostable crash).
