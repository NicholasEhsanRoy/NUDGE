# 000000028 · uq-fixer · P7 (lyapunov multi-point breaker identifiability gate — LIM-025)

*Immutable run record (write-once). The live pointer is the newest record's `NEXT →` block
+ its mirror in `../LEDGER.md`.*

## ▶ NEXT →

```
NEXT  →  nudge-audit  on  the P7 fix (commit d4db2b0)
then  →  orchestrator: independently verify + merge iff AUDIT PASS
then  →  nudge-red-team (re-scan) → STOP at HOLES_FOUND: 0
```

## Role / target

- **Role:** `nudge-uq-fixer` (role 3), worktree-isolated on branch
  `fix/lyapunov-multi-uq-gate` (based off `main` = `bea3b93`). `main` untouched.
- **Target:** **P7** — `nudge.inference.lyapunov.attribute_lyapunov_multi` (M3). New
  `NUDGE-LIM-025`. Finding `runs/000000027`.

## What it did (fix claim, condensed)

- **Independently reproduced** (shipped `attribute_lyapunov_multi`, LNA-mixture toggle,
  SCALE=20/OBS_SD=0.5, 2 operating points bB∈{0.05,0.30}, 3000 cells, 2 seeds): gain **n=1.5 →
  confident-wrong `threshold`, gap ≈1.70** (2/2). Monostable bB=0.50 → `ValueError` in
  `calibrate_from_wt`.
- **Task-1 fork resolved (measured):** the n=1.5 perturbed condition is **bistable at bB=0.05
  (prox 0.539, at the fold) but MONOSTABLE at bB=0.30** — a genuine confident-wrong on a
  degenerate 2nd operating point (K=2.0 is ALSO monostable-perturbed at both points yet resolves
  correctly, so monostability alone does not separate them).
- **Task-1 curvature diagnosis (measured):** the naive "degenerate joint fit ⇒ abstain" is
  **FALSIFIED** — joint (Δn, ΔK) Laplace: CORRECT K=2.0 cond ≈1000–1300 (gain a free nuisance,
  unidentifiable) vs WRONG n=1.5 cond ≈7 (both identifiable). The RIGHT statistic (STATE.md
  gotcha #4): after resolving winner X, fit the joint (X, runner-up Y) two-mechanism model and
  read whether Y is IDENTIFIABLE and DISPLACED from no-change. Measured CONTAMINATION
  (`|log Y* − log Y_nom|` if Y identifiable, via `uncertainty.laplace_posterior`): genuine
  K=2.0 → 0.095; genuine ceiling vmax=1.5 → 0.114/0.116; **n=1.5 hole → 1.042/1.013**. Cut
  `_CONTAM_MARGIN=0.5` sits centred in the ≤0.12↔≈1.0 gap.
- **The fix (additive; `src/nudge/inference/lyapunov.py` ONLY — frozen `fit.py`/`core/`
  untouched):** (1) an **identifiability gate** (`_resolution_contamination` +
  `_fixed_root_multi_loss` + `_fit_fixed_root`) — after a bare mechanism resolves, abstain when
  any runner-up mechanism is identifiable and displaced beyond `_CONTAM_MARGIN`, regardless of
  the NLL gap; an unreadable curvature ⇒ abstain (fail-safe). (2) **graceful monostable
  degradation** — an up-front `bifurcation_proximity is None` guard + a `try/except ValueError`
  wrap around the joint fits → `("unresolved", {})` ("bistability lost"), never a stack trace.
- **Status:** **CLOSED (0 confident-wrong) / BOUNDED (honest).** n=1.5 → `unresolved` (2/2);
  K=2.0 → `threshold` (positive control, no over-abstention); n=2.4 → `unresolved`; monostable →
  graceful. Residual BOUND (locked strict-xfail): a genuinely-degenerate large-gain perturbation
  can only be ABSTAINED, not positively resolved to `gain` — recovering a positive gain call
  needs a better-buffered operating point.
- **Honesty record:** `NUDGE-LIM-025` (new, schema-valid, severity major / safety_relevant);
  `scripts/vv/FINDINGS.md` §P7 (measured before/after tables); `attribute_lyapunov_multi`
  docstring, STATE.md (§6 + the red-team row), CHANGELOG `[Unreleased]`, README red-team para all
  updated.
- **Regression lock (`tests/inference/test_lyapunov_multi_uq_gate.py`):** the n=1.5
  confident-wrong abstains (2 seeds); K=2.0 still resolves; n=2.4 still abstains; monostable →
  graceful; a strict-xfail decoy locks the residual over-abstention bound. The pre-existing
  `test_lyapunov.py::test_second_operating_point_breaks_confound` still passes.

## Branch + commit (NOT merged by the fixer)

- Branch: `fix/lyapunov-multi-uq-gate` · Commit: `d4db2b0`.

## Gate (fixer-reported; to be independently re-checked by audit)

- ruff (lyapunov + test + repro) — passed · pyright `src/nudge/inference/lyapunov.py` — 0/0/0 ·
  5 doc checkers (anomalies / citations / impl_mapping / mechanism_cards / hardening_append_only)
  — OK.
- `tests/inference/test_lyapunov_multi_uq_gate.py` + the existing breaker test →
  **8 passed, 1 xfailed** (264s).
- red-team repro `scripts/redteam/lyapunov_multi_gain_threshold_hole.py 2` → **HOLES: 0**.
- Full fast lane `pytest -q` → 350 passed, 5 skipped, 2 xfailed (no regression).

## Commands the audit should re-run

```
uv run ruff check src/nudge/inference/lyapunov.py tests/inference/test_lyapunov_multi_uq_gate.py scripts/redteam/lyapunov_multi_gain_threshold_hole.py
uv run pyright src/nudge/inference/lyapunov.py
uv run python scripts/check_anomalies.py
uv run python scripts/redteam/lyapunov_multi_gain_threshold_hole.py 2          # expect HOLES: 0
uv run pytest -q tests/inference/test_lyapunov_multi_uq_gate.py tests/inference/test_lyapunov.py::test_second_operating_point_breaks_confound -m "slow or not slow"
```
